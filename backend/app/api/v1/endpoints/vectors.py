"""
ベクトル検索・クラスタリング関連のAPIエンドポイント
注意: スクリプト生成サービスが内部的にベクトル検索機能を使用しているため、
サービス層は保持し、不要なAPIエンドポイントのみを削除しています。
"""
from fastapi import APIRouter

router = APIRouter()

# すべてのエンドポイントを削除
# ベクトル検索機能はスクリプト生成サービス内で内部的に使用されるため、
# 外部APIとして公開する必要はありません。